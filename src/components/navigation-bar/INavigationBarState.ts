/** Navigation Bar component state */
export interface INavigationBarState {
  /** Flag to indicate if import items button is enabled */
  importItemsButtonEnabled: boolean;
  /** Flag to indicate if logout button is enabled */
  logoutButtonEnabled: boolean;
}
